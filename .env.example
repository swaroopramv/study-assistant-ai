# Copy this file to .env to override defaults (all optional for local Ollama)

OLLAMA_BASE_URL=http://localhost:11434
CHAT_MODEL=ministral-3:8b
# Embedding model (run `ollama pull nomic-embed-text` once)
EMBED_MODEL=nomic-embed-text
